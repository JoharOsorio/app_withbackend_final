import 'package:app_withbackend_final/models/category.dart';
import 'package:app_withbackend_final/theme/theme.dart';

class CategoryCard extends StatelessWidget {
  final ListCategories product;
  const CategoryCard({Key? key, required this.product}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(
     
    );
  }
}